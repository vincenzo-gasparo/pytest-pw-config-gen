# Generated by pytest-pw-config-gen — edit freely
# Re-run the generator to regenerate this file.
import pytest


@pytest.fixture(scope="session")
def browser_context_args(browser_context_args):
    return {
        **browser_context_args,
        "viewport": {"width": {{ viewport_width }}, "height": {{ viewport_height }}},
{% if action_timeout is not none %}        "default_timeout": {{ action_timeout }},  # ms; per-action timeout
{% endif %}{% if locale is not none %}        "locale": "{{ locale }}",  # BCP 47 language tag e.g. "en-US", "it-IT"
{% endif %}{% if timezone_id is not none %}        "timezone_id": "{{ timezone_id }}",  # IANA timezone e.g. "Europe/Rome"
{% endif %}{% if geolocation_lat is not none %}        "geolocation": {"latitude": {{ geolocation_lat }}, "longitude": {{ geolocation_lon }}},
{% endif %}{% if effective_permissions %}        "permissions": [{% for p in effective_permissions %}"{{ p }}"{% if not loop.last %}, {% endif %}{% endfor %}],
{% endif %}{% if storage_state is not none %}        "storage_state": "{{ storage_state }}",  # path to saved auth state JSON
{% endif %}{% if color_scheme is not none %}        "color_scheme": "{{ color_scheme }}",  # "light", "dark", or "no-preference"
{% endif %}{% if accept_downloads %}        "accept_downloads": True,
{% endif %}{% if http_credentials_user is not none %}        "http_credentials": {"username": "{{ http_credentials_user }}", "password": "{{ http_credentials_pass }}"},  # HTTP auth
{% endif %}{% if extra_http_headers %}        "extra_http_headers": { {% for k, v in extra_http_headers.items() %}"{{ k }}": "{{ v }}"{% if not loop.last %}, {% endif %}{% endfor %} },
{% endif %}{% if user_agent is not none %}        "user_agent": "{{ user_agent }}",
{% endif %}        "ignore_https_errors": {{ ignore_https_errors | python_bool }},
    }
